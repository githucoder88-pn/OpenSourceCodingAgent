# Tools

All capabilities are tools with schema, description, permissions, timeout, cancellation, structured result, errors, telemetry.

Initial: read_file, write_file, edit_file, create_file, delete_file, list_directory, search_files, search_symbols, shell, git_status, git_diff, git_log, git_add, git_commit, git_branch, run_tests, run_build, run_linter, inspect_environment, http_request, browser, python, node, package_manager
