# Forge Tests

- Unit tests: scheduler, task graph, router, permissions, context, memory, event bus, protocol
- Integration: agent loop, tool execution, multi-agent communication, subagent spawning, task dependencies, model failover, session resume, checkpoints, rollback, Git workflows
- E2E: User → Core → Agent → Tool → File → Test → Result
